targets: [iOS, android]
android:
  lib: libfreetyped.a
  ndk:
    runtimes: [c++, gnustl, stlport]
    platforms: [12, 21]
iOS:
  lib: libfreetyped.a
example: |
  #include <ft2build.h>
  #include FT_FREETYPE_H
  int main() {
    FT_Library library;
    FT_Error error;
    error = FT_Init_FreeType(&library);
    if (error) {
      return EXIT_FAILURE;
    }
    return EXIT_SUCCESS;
  }

